// import { generarJwt } from "../helpers/generarJwt.js";

// describe("generar json web tokens", () => {
//   test("test generate token", () => {
//     const id_usuario = 2;

//     const token = generarJwt({ id_usuario }).then((e) => e);
//     console.log(token);
//     expect(token).resolves.toBe("");
//   });
// });

// TODO: hacer un test para verificar que el token sea valido
